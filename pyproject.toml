[project]
name = "fly-in"
version = "0.1.0"
description = "An efficient drone routing system that navigates multiple drones through connected zones."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
fly-in = "fly_in:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
